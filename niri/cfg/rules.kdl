    window-rule {
        geometry-corner-radius 20 // Set every window radius to 20
        clip-to-geometry true
        default-column-width {} // Use every window's own preferred size
    }

    window-rule {
        match title="Sinmai"
        open-floating false
    }

    window-rule {
        match title="Hold on..."
        open-floating true
    }

    layer-rule {
        match namespace="^noctalia-wallpaper*"
        place-within-backdrop true
    }
